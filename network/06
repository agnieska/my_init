#! /bin/sh

echo "\n6. Récupérez le path complet du fichier dans lequel est écrit l’adresse IP du serveurDNS que vous utilisez"
echo "\nDEDUCTION :" 
echo 'The file etc/resolv.conf  points to another file /var/run/resolv.conf'
echo 'The second file is automatically generated at each restart of machine (daemon)'
echo "It contains the IP address of the used DNS server nameserver 10.0.1.1\n"
ls -la /etc/resolv.conf
ls -la /var/run/resolv.conf
echo "cat /var/run/resolv.conf"
cat /var/run/resolv.conf


